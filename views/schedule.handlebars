<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table>

    {{!-- <tbody>
				{{#each kittens}}
				<tr>
					<th scope="row">{{id}}</th>
					<td>{{name}}</td>
					<td>{{days}}</td>
					<td>{{arrivingOn}}</td>
				</tr>
				{{/each}}
				
			</tbody> --}}
    </table>
</body>
</html>